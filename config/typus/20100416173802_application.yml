# Typus Models Configuration File
#
# Use the README file as a reference to customize settings.

Category:
  fields:
    list: name, description, created_at
    form: name, description
  order_by:
  relationships: services
  search:
  application: Services